# Codex publication check — the T2 and T3 page sections and runbook §7.2–7.3 (`feat/bench-m6`)

| | |
|---|---|
| Date | 2026-09-26 |
| Scope | `docs/benchmarks.md` § T2, § T3 (prose, tables, disclosures); `evals/benchmark_it_with_claude.md` §7.2, §7.3; the archive under `evals/results/docsqa/T2/`, `T3/sizes.json`, `T2/FROZEN.md` |
| Reviewer | Codex CLI via the shared companion runtime, `gpt-6-astra`, thread `01a0dcca-4d5e-7691-a7da-b7d364eb8f25`, read-only, synchronous |
| Pinned to | `f692869` |
| Triaged by | Claude (Fable 5.1), same day, before the merge |
| Outcome | 14 findings (2 High, 7 Medium, 5 Low), all text: **14 accepted; 12 applied to the page and runbook, 2 deferred to the M7 renderer change** (the T3 renderer prints "MB" for MiB and labels the reconstruction step "attach + embed"; changing `t3.sh` after the T2 runs would mean a third freeze rewrite postdating every row, so the page and runbook state the unit and the step's content instead). All 34 hashes of §7.2 verified by Codex; "no savings are claimed" confirmed correct. Verdict: "publishable after these text changes". |

## Findings and what changed

| # | Sev | Finding | Applied |
|---|---|---|---|
| 1 | Med | Consumption prose quoted question-level medians as run medians. | Ranges now the table's run medians ($0.029–0.036 mda, $0.039–0.043 grep, $0.045–0.048 qmd). |
| 2 | Med | The totals line omitted the failed first attempts' spending and its boundary. | "$49.74 for the final attempts, $50.42 including the failed first attempts; excludes grading and panel calls"; generated from the committed `grades.jsonl` run metrics. |
| 3 | Med | Grounding-cause counts did not match the archived indicators (overlapping categories). | Indicator counts with their definitions and denominators (explicit ungrounded batch 75/86/53/39; unresolved 28/38/24/1; oversized 11/1/5/3; no citation 1 mda, 5 grep; one mda answer without a verdict); the "a third to a half" sentence removed. |
| 4 | Low | Tailwind mda vs qmd gate (a) already passed on the original grades. | "Newly passes for mda vs grep on Supabase; Tailwind's mda vs qmd stays passing (lower bound −0.12 → 0.00); adjudicated Supabase mda vs graphify excludes zero." |
| 5 | Low | Astra's offset stated as 2–3 points; the range is wider. | "1.87–3.27 below Sonnet, 2.03–3.77 below Fable." |
| 6 | Med | Both T3 cost ranges omitted an endpoint. | "$3.76 to $4.66" (mda); "$0.07 to $1.52" (graphify-haiku, successful attempt). |
| 7 | Low | Sizes are MiB, printed as MB. | Prose says MiB; the renderer's label is deferred to M7 and the page says so. |
| 8 | High | "Every paired interval includes zero … about equally well" overstated parity. | "Differ from grep by at most 0.10 points; every original-grade interval includes zero, but that does not establish equivalence: most pairs fail the declared non-inferiority criterion." |
| 9 | Med | T3's "first build" framing overstated completeness; "attach + embed" includes scoring. | The section states what the timings are (reconstruction: attach + embed + score; summarisation wall-clock unrecorded; single observations; descriptive across configurations). Column labels deferred to M7. |
| 10 | Low | Card-audit percentages lacked their denominator. | "Among values covered by valid member verdicts, 96.2–99.6% … invalid verdicts excluded; a sample audit does not verify every stored value." |
| 11 | Med | The citation policy was not explicit on the page. | The full rule and cut-offs stated in the disclosures. |
| 12 | Low | §7.2 said 25 questions for every sample. | 25 / 25 / 25 / 21 (Supabase). |
| 13 | High | §7.2's regeneration claim was not runnable (`--json`, comparators, `--adjudicated` is `t2.sh`'s, embedded paths). | A working four-project loop with `--json`, the manifest's comparators, draws and seed, provenance-path normalisation, plus the rendering commands for the runs, panel and card tables. |
| 14 | Med | §7.3 lacked the rendered table's hash and the remeasurement boundary. | The `t3.sh table` output hash (`4c38b4b7…`), "do not run `sizes` during a regeneration", version dependency stated. |
