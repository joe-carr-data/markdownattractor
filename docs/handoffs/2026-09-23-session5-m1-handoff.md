# Handoff — 2026-09-23, session 5 (benchmark M1: card export, freeze, preflight, arm scorer; first carded rows)

Written by Claude (Fable 5.1) during the fifth build session, for the next session after compaction. **Read this whole file first, then `2026-09-23-session4-final-handoff.md` §3 for the reading list (still valid) and §4–§5 for recipes and caveats.** Everything below was verified in-session unless marked otherwise.

Claude Code session: https://claude.ai/code/session_016dTZj7CmBSJeFLDCwUCxEF

## 0. One-paragraph state

Milestone M1 of the execution plan (`docs/plans/2026-09-benchmark-execution.md` §5) is implemented on branch `feat/bench-m1`: the four DocsQA corpora's cards are committed (`evals/results/docsqa/cards-0.1.1-<project>.json` + `.provenance.json`, 38 MB), `model.sha` hashes the embedding model files and the snapshot links, `mda eval --dataset docsqa` gained `--export-cards`, `--arm-output` and `--arm-name`, `results.json` archives ten pages per question, and `scripts/eval/{lib,freeze,preflight,probe,table}.sh` exist. The **first carded and hybrid development rows at full coverage** are on `docs/benchmarks.md` (success@5 raw → cards → hybrid: github-docs 0.306 → 0.388 → 0.408, prisma 0.216 → 0.297 → 0.297, supabase 0.333 → 0.417 → 0.500, tailwind-css 0.600 → 0.840 → 0.800). Codex reviewed M1 (`docs/reviews/codex/2026-09-23-bench-m1.md`, 10 findings, 9 fixed, 1 in part); the freeze was rewritten on the fixed scripts (`evals/results/docsqa/FROZEN.md`, source commit `bb26333`); the four preflights were rerun under it (§2). qmd 2.8.3 and graphify 0.9.66 are installed on this machine; qmd's models are pulled.

## 1. Branch, commits, PR

| Item | State |
|---|---|
| Branch | `feat/bench-m1` from `main` at `f1f632d` (merge of #21). Commits: `177a533` feat(eval) export + scorer; `ff3c6b9` cards, results, scripts; `09bdec8`/`e388c65`/`2f5f2bb` freeze fixes; `c419324` docs; `af57e8e` tailwind preflight (old script, superseded); `9984d00` **Codex fixes**; `7a09697` results with `pages`, model.sha; `f019e8e` freeze; `f3f517d` rank cap; `bb26333` re-freeze; `4e55e70` preflight reports; `0158edd` github-docs attempt 2 + this handoff. |
| PR | **#22** `feat/bench-m1` → `main` (https://github.com/joe-carr-data/markdownattractor/pull/22). A detached watcher (`~/.cache/markdownattractor/bench/pr22-merge.sh`, log `pr22-merge.log`) merges it on eleven green jobs; if it did not, read the job list (`gh run view <id> --json jobs`), never the checks column, then `gh pr merge 22 --merge`. |
| Codex | Thread `01a0cce6-aafc-78b1-ba5f-b857f97230ca` (M1 review, `--fresh --model gpt-6-astra`, pinned `af57e8e`). Plan thread `01a0ccb1-a678-7041-9dad-1f08485d767f` unchanged. Always write "do NOT spawn a nested `codex exec`; the companion thread IS the reviewer and answers directly". |
| Gate | `make check` green (301 tests) on `9984d00`+; one test, `worker::tests::fake_binary::timeout_kills_the_child_and_is_retryable`, failed twice under machine load (a preflight embedding 8K cards at the time) and passed alone each time: a timing margin to widen, not an M1 change. |

## 2. Preflight outcome (development protocol, `scripts/eval/preflight.sh development <project>`)

Reports: `evals/results/docsqa/preflight/development-<project>.json`, traces under `evals/results/docsqa/preflight/development-<project>/probes/`, attempt directories under `~/.cache/markdownattractor/bench/preflight/development/<project>/<timestamp>/`. Checks: env, build (the executable Cargo reports for a `--locked` release build, sha256 recorded), frozen, model, store, regenerate (archived page lists through `--arm-output`, no store), replay (the committed store rerun), reconstruction (clean copy + committed cards + re-embed, identical rows and inventory), coverage-grep, probes-mda, probes-grep.

Under the **previous** freeze (old scripts, before the Codex fixes) all checks passed on tailwind-css, supabase and prisma, including the reconstruction (identical on every row and question; attach+embed+score 146 s / 836 s / 1,721 s). The github-docs run under the old script was interrupted after two seconds and **wrote `passed: true`**: exactly Codex F1, now fixed (an interrupted run fails and records the checks that did not run). The rerun under the new freeze: **see the reports in the repository; the table below is filled at the end of the session.**

| Project | Result | reconstruction (index s / attach+embed+score s) |
|---|---|---|
| tailwind-css | 11/11 passed | 0 / 117 |
| supabase | 11/11 passed | 1 / 612 |
| prisma | 11/11 passed | 2 / 975 |
| github-docs | 11/11 passed (attempt 2; attempt 1 10/11, see below) | 7 / 2165 |

The github-docs first attempt under the new freeze (`~/.cache/markdownattractor/bench/preflight/development/github-docs/20260923T074625Z/`, report copy there) passed every check but `probes-grep`: on question `github-docs::118629` the grep arm made eleven successful Grep calls and hit the 12-turn budget without answering (rule 0.3: a run error). The probe rule requires a successful run, so the preflight was rerun once (attempt `20260923T083238Z`): 11/11, the same question answered in 4 turns. The first attempt stays on record; the grep arm's turn budget on a 3.7K-file corpus is something T2 will see in the failure counts (rule 0.3), not something to tune away.

## 3. What changed in the code and why (M1)

- `crates/mda-core/src/eval/mod.rs`: `Metrics.mean_ms: Option<f64>` (an external arm has no latency column, plan §2.7).
- `crates/mda-core/src/eval/docsqa.rs`: `QuestionResult.pages` (first ten distinct pages, was `top` with five: the metrics regenerate from the file, Codex F9); `Run.missing`/`Run.unknown`; `ArmRow`, `read_arm_output` (duplicate id and empty path refused), `normalize_arm_path`, `score_arm` (same `eligible`, split, holdout, `pages_of`, `score_pages` as `evaluate`).
- `crates/mda-core/src/store/mod.rs`: `Store::sections_carded` (sections, not hashes: the coverage unit a row needs).
- `crates/mda-cli/src/commands/eval.rs`: `--export-cards` (one card per line, sorted hashes, provenance sidecar, refuses the checkout, no symlink), `--arm-output`/`--arm-name`, `card_coverage` in the report; tests in `engine_cli.rs` (export → clean copy → identical; arm scoring; archived pages → identical).
- Scripts: `lib.sh` (locations, `sha256` fail-closed, `ident`, `safe_target`, `model_sha` with links, `normalize_runs`, `archived_rows`, `probe_ids`, `question_text`, `question_split`), `freeze.sh` (Inputs fail-closed; checkouts verified clean; effective `config.toml` hashed; `.git/info/exclude` rules recorded; `--check` diffs Inputs and requires the code paths `crates prompts skills scripts/eval Cargo.lock Cargo.toml rust-toolchain.toml .cargo` unchanged since the frozen commit), `preflight.sh` (see §2), `probe.sh` (a call counts only with a non-error `tool_result`; holdout refused; launch configuration recorded), `table.sh` (the page table from the raw results).

## 4. Numbers and what they say

`docs/benchmarks.md` "carded and hybrid rows at full coverage (development)": cards move every project (+0.08 to +0.24 success@5 over raw); vectors help GitHub Docs and Supabase, do nothing on Prisma and cost Tailwind 0.04. The adapter's latency (0.5–1.6 s per question in-process, release build: the page rule fetches deeper and hybrid embeds the query on each fetch) is the loudest number and a product lever before axis B. Every number on the page is rendered by `scripts/eval/table.sh`; the deltas in prose were computed by `jq` from the same files.

## 5. M2 groundwork (branch `feat/bench-m2`, worktree `~/mda-worktrees/m2`, based on `feat/bench-m1`; rebase onto `main` after M1 merges)

Commits there: `f12e4a9` qmd arm build/drive + `scripts/eval/bm25-files.sh`; `5111991` `crates/mda-cli/examples/mcp_time.rs` + `scripts/eval/mcp-time.sh`; `7ee47b3` mcp_time `--list-tools` and `MCP_TIME_DUMP`; `85934be`, `1fa5abf` qmd record and MCP drive. Clippy clean (`CARGO_TARGET_DIR=~/mda-worktrees/target`). Not yet: probes for the qmd/graphify arms in `probe.sh`, the graphify arm script, arms in `FROZEN.md` (`freeze.sh` already folds `evals/results/docsqa/arms/*.json` in), docs.

- **qmd 2.8.3** installed (`npm i -g @tobilu/qmd@2.8.3`, Node 22.17.1); models pulled to `~/.cache/qmd/models/` (EmbeddingGemma-300M Q8, qmd-query-expansion-1.7B Q4, Qwen3-Reranker-0.6B Q8). One index per project: `qmd --index <project>` (`~/.config/qmd/<project>.yml`, `~/.cache/qmd/<project>.sqlite`). **Default mask is `**/*.md`; the arm uses `--mask '**/*.{md,mdx,markdown}'`** (documented option; published). Tailwind index built once (embed 161 s under load; rebuild for a clean T3 number): 198 files, 1,007 chunk vectors, 197 of 197 corpus pages. The MCP `query` tool: `{query, limit (default 10), rerank (default true), candidateLimit (40), searches[{type: lex|vec|hyde, query}], collections, intent, minScore}`; results in `structuredContent.results[] {file: "<collection>/<rel path>", docid, line, score, title, snippet}`, one entry per document. `scripts/eval/arms/qmd.sh drive <project> full|no-rerank|bm25 <out.jsonl>` goes through the MCP tool with `mcp_time` (the timed call is the scored call; limit 20 then 40; `truncated` when ten pages are not reached at 40, the reranker's candidate limit); `build` and `record` write `evals/results/docsqa/arms/qmd-<project>.json`.
- **qmd's lexical search ANDs every term and has no OR fallback**: on the long community questions `qmd search` / a lex-only MCP query returns nothing (0 of 25 Tailwind dev questions), so the "qmd BM25" ablation row is ~0 on this dataset by qmd's own design; the full mode's query expansion is what makes qmd work on these questions. Say so on the page; do not reformulate the questions for it (that would be our query engineering).
- **qmd full runs** once the owner's `llama-server` was stopped (owner instruction, 2026-09-23 09:2x UTC: "stop llama-server and retry"): with it running, every Metal query failed out of memory and CPU mode took 5–6 min per question. Now `query` with reranking takes ≈ 60 s per question on this M3 (cold 59 s incl. 0.4 s startup; warm 65 s): the 1.7B expansion model plus the reranker. The M2 qmd run (`scratchpad/m2/qmd-all.sh`, log `qmd-all.log`; outputs `~/.cache/markdownattractor/bench/qmd-runs/<project>-<mode>.jsonl`) builds the three remaining indexes then drives full, no-rerank and bm25 for the four projects (≈ 173 dev questions × 60 s per mode for full). `probe.sh qmd <project> <id> <trace>` works (qmd's MCP server plus its own skill as instructions; trace shows `mcp__qmd__query` then `mcp__qmd__get`).
- **BM25-over-files control** (`scripts/eval/bm25-files.sh build|drive`): python3 sqlite3 3.43.2 FTS5 over whole pages, unicode61 remove_diacritics 2, mda's query form (quoted terms AND, OR fallback). Dev-split success@5 (rows and scorer output under `evals/results/docsqa/<project>/arms/bm25-files.*`): tailwind-css 0.640 · supabase 0.333 · prisma 0.243 · github-docs 0.265 — above mda's section-level raw row on Tailwind and Prisma, equal on Supabase, below on GitHub Docs; well below cards + raw everywhere.
- **`mcp_time`** (`cargo build --release --example mcp_time`): one rmcp client for every arm's latency (cold first call with `startup_ms`, warm after), `--list-tools`, `MCP_TIME_DUMP` for the results. mda on Tailwind, `mda_search` k=10: startup 48 ms, cold call 521 ms, warm 218–770 ms.
- **graphify 0.9.66** (`uv tool install "graphifyy[mcp]==0.9.66"`; `graphify-mcp <graph.json>` serves `query_graph`, `get_node`, `get_neighbors`, `get_community`, `god_nodes`, `graph_stats`, `shortest_path` + PR tools). The arm (`scripts/eval/arms/graphify.sh build|drive`) builds on a COPY of the checkout (`$RUN/graphify/<project>/src`) with `graphify install --project --platform claude` inside it (skill, PreToolUse hook-guards, CLAUDE.md nudge; archived as `installed-dot-claude/`) and a headless `claude -p "/graphify <copy> --no-viz"` with `--setting-sources project` (an isolated `CLAUDE_CONFIG_DIR` has no login). Two traps found and fixed: (1) the first session found `GEMINI_API_KEY` in the shell and extracted through Gemini, then `pip install openai` and `uv tool install graphifyy[gemini] --force` (which dropped the `mcp` extra): every arm script now `unset_provider_keys` and the prompt forbids keys, installs and reinstalls; the tool was reinstalled as `graphifyy[mcp]==0.9.66`; (2) the skill's "narrow the corpus above 500 files" question is pre-answered in the prompt (whole path). **Tailwind build**: 825 s, 83 turns, 48 subagents, 5.35M input tokens, $10.25 list-price equivalent, `claude-sonnet-5`; 566 nodes / 507 edges (networkx `links`); 197 of 197 corpus pages have a node. `query_graph` returns text (`NODE <label> [src=<file> …]`, cut at a 2,000-token budget with a `[!] TRUNCATED` marker); the driver takes every NODE's src in order (frozen mapping), second pass at `token_budget` 8000 when under ten pages, `truncated` if still short. **Tailwind dev row: success@5 0.520, MRR@5 0.288, nDCG@10 0.377** (`evals/results/docsqa/tailwind-css/arms/graphify.*`), below mda raw. Probe works with the hook live (Grep blocked until `query_graph` ran). Builds for supabase, prisma, github-docs were launched detached (`scratchpad/m2/graphify-all.sh`, log `graphify-all.log`).
- **First competitor rows on the dev split (development, `evals/results/docsqa/<project>/arms/`)**: Tailwind — qmd full (MCP `query`, rerank on, limit 20) **success@5 0.720, MRR@5 0.396, nDCG@10 0.559**, no truncation, median 62 s per query on Metal (mda cards + raw 0.840, hybrid 0.800, BM25-over-files 0.640, graphify 0.520); Supabase — graphify **0.083** (1 of 12), build 2,053 s, 264 turns, 64.6M input tokens ($47 list-price equivalent through the login), 96% of corpus pages have a node. The remaining qmd drives (supabase/prisma/github-docs full, then no-rerank and bm25 for all four; `qmd-all.log`) and the graphify builds for prisma and github-docs (`graphify-all.log`; github-docs will be ≈ 5× Supabase's token volume) were running detached at the end of the session: score each `~/.cache/markdownattractor/bench/{qmd,graphify}-runs/<project>-<mode>.jsonl` with `mda eval … --arm-output … --out`, copy rows, results, request and times under `evals/results/docsqa/<project>/arms/<arm>.*` as done for Tailwind, then three probes per new arm per project, `freeze.sh` (the arms section now lists `qmd`, `graphify`, `bm25-files`), a re-freeze, and `preflight.sh` for the four projects (reconstruction included: ≈ 1.5 h) — that is M2's exit.
- **qmd full on all four dev splits** (rows under `<project>/arms/qmd-full.*`, no failed calls, no truncation, median ≈ 60 s per query):

| Project | qmd full success@5 | mda cards + raw | mda hybrid |
|---|---|---|---|
| tailwind-css | 0.720 | 0.840 | 0.800 |
| supabase | 0.417 | 0.417 | 0.500 |
| prisma | 0.297 | 0.297 | 0.297 |
| github-docs | 0.429 | 0.388 | 0.408 |

  qmd full wins GitHub Docs (0.429 vs 0.408), ties Prisma and Supabase's cards row, loses Tailwind. **no-rerank**: 0.640 / 0.250 / 0.243 / 0.327 (tailwind/supabase/prisma/github-docs; the reranker is worth +0.08 to +0.17); **BM25 (lex-only)**: 0.000 / 0.000 / 0.027 / 0.000 (AND of every term, no OR fallback). All rows archived under `<project>/arms/qmd-{full,no-rerank,bm25}.*` and on `docs/benchmarks.md` (competitor section, development).
- **The owner's Claude login hit its weekly limit at ≈ 12:20 UTC on 2026-09-23** ("You've hit your weekly limit · resets Sep 28 at 6am (Asia/Dubai)") while the graphify builds for Prisma (attempt 2, 69 turns, 38 $-equivalent) and GitHub Docs (98 turns, 55 of 158 chunks, 74 $-equivalent) were running: both ended without a graph and are recorded as *did not complete* in their arm records (rule 0.3). Single `claude -p` pings still answered afterwards; whether a real run works is tested by a grep probe (see the status below). Until the reset, nothing that needs the login can run: graphify builds, activation probes, hence the preflight's probe checks, and T2. Everything local continues: qmd drives, BM25-over-files, `mcp-time.sh`, scoring, freeze, and `preflight.sh … --skip-probes` (which records the probes as skipped and cannot pass). graphify's extraction cache under `<copy>/graphify-out/cache` should let a rerun after the reset reuse the finished chunks (verify: the skill's Step B0 checks the cache).
- **Build-cost claim, discussed with Codex** (`docs/reviews/codex/2026-09-23-build-cost-claim.md`, thread `01a0ce81-512a-7cb2-b83a-3451001c57da`): the bounded, hash-keyed, per-section build is a defensible design property; "cheaper indexing than the alternatives" is **not** established by today's numbers (mda's list-price equivalent is lower than graphify-Sonnet's on Tailwind and Supabase but graphify is faster; different models and artifacts; subscription weighting of cache reads vs output unknown; the parent's polling turns are partly our headless harness; graphify also caches extraction; qmd has zero remote tokens). Actions taken: the `graphify-haiku` configuration is being built on fresh copies for all four projects (prisma and github-docs first, `scratchpad/m2/graphify-haiku.sh`; tailwind and supabase to follow) as the model-matched arm; the record now stores usage per model and per token category, failure provenance, the coverage population, and a gzipped durable copy of each graph under `arms/graphs/`; runbook §4c states the T3 accounting Codex asked for and the accepted descriptive wording; the plan's T4 line must say `/graphify --update` (skill flow, model pass), not `graphify update` (code only). The weekly-limit event is publishable as an operational event with its timestamp and message, never as "consumed the whole plan".
- **mda MCP latency** (`mcp-time.sh`, k=10 hybrid, one server per project, `~/.cache/markdownattractor/bench/latency/mda-<project>.jsonl`): warm median 177 / 360 / 538 ms on tailwind / supabase / prisma (github-docs in the same log); cold first call 325 / 783 / 1,588 ms with a 14–16 ms process start. qmd's per-question first-pass latency is in `<project>/arms/qmd-*.times.jsonl` (full ≈ 60 s, no-rerank ≈ 10 s).
- **graphify-haiku built on all four projects** (6–8 min each, $0.97–$6.14 equivalent): success@5 0.040 / 0.000 / 0.000 / 0.061. Codex corrected my first reading: the graphs are *not* "mostly unsourced" (Prisma 625 and GitHub Docs 3,614 distinct source files; Tailwind 82, Supabase 108), and few `Agent` calls do not prove skipped chunks; whether the extraction ran as the skill prescribes needs a chunk-level audit (follow-up). Recorded as its own configuration; it does not replace the Sonnet arm and does not give the model-matched build-cost comparison yet.
- Probes exist for four arms now (`probe.sh mda|grep|qmd|graphify`); `freeze.sh` lists each competitor arm once with its per-project records; `preflight.sh` refuses to certify with a provider key in the environment.

## 5b. M2 state at the end of the session

Branch `feat/bench-m2` (worktree `~/mda-worktrees/m2`, rebased on `main` = merge of #22), ≈ 25 commits: the arm scripts, `mcp_time`, probes for five arms, the freeze with the Arms section, arm records for qmd (4), graphify-Sonnet (2 complete + 2 did-not-complete), graphify-haiku (4), bm25-files (4), rows and scorer output for every arm and project under `<project>/arms/`, gzipped graphs under `arms/graphs/`, the page section, runbook §4c, three Codex files. The **M2 development freeze** (`FROZEN.md`, source commit `d2dfc32`) was written and checked; the four preflights were launched under it from the worktree (`scratchpad/m2/preflight-m2.log`; reports land in `evals/results/docsqa/preflight/`; each project probes mda, grep, qmd, graphify, graphify-haiku; a probe is *not applicable* where the arm's build did not complete). A Codex review of M2 was requested at `62ffbc1`. **To finish M2**: read the preflight reports (expect 15 checks per project), commit them, triage the Codex review into `docs/reviews/codex/2026-09-23-bench-m2.md`, fix, re-freeze if a code path changed (then rerun the preflights), open the PR, merge on eleven green jobs. Environment rules that bit today and are now enforced by the scripts: provider keys unset (`unset_provider_keys`), the GPU free for qmd, `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0` and no wakeup/cron tools for headless graphify builds, `env -u OPENAI_API_KEY -u GEMINI_API_KEY` when launching the preflight from this shell.

## 5c. M3 state (branch `feat/bench-m3`, on `main` after #23; PR #24 opened 2026-09-24, merge watcher `~/.cache/markdownattractor/bench/pr24-merge.sh`)

- **Tuning knobs** (`config.toml`: `embedding_text` v1 | questions-first | with-entities | questions-first-with-entities; `search_rrf_k`; `search_raw_weight`; `search_questions_weight`; `search_and_stopwords`), read by `SearchOptions::for_config` at every entry point; a text variant changes the vector model id (`bge-small-en-v1.5-q+questions-first`) so vectors are rebuilt and never mixed; `docs/design/search.md` table. Codex reviewed (`docs/reviews/codex/2026-09-23-bench-m3-tuning.md`, 8 findings fixed).
- **The loop**: `scripts/eval/tune.sh baseline|trial|keep|base` (restoration trap, persisted base incl. `fetch=`, required hybrid row, decisions verified, trials archived under `evals/results/docsqa/tuning/<trial>/` with manifest + four `results.json`; ledger `evals/results/docsqa/TUNING.md`), driven by `scratchpad/m3/tune-all.sh` in the plan's order (c1 questions-first; c2 with-entities on top of the winner; c3 questions weight 3; c4 raw weight 0.7; c5 RRF k 30; c6 fetch 60; c7 stop-words; c8 not run without the diagnostic and an ADR). Baseline reproduced the committed hybrid rows exactly (objective 0.5014). The first candidate's re-embedding (≈ 1 h) is kept in the stores under its model id, so its rerun on the fixed script is fast.
- **Loop outcome**: baseline 0.5014 (reproduces the committed hybrid rows); c1 questions-first 0.4916 (GitHub Docs +0.04, Tailwind −0.08) and c2 with-entities 0.4824 (Prisma +0.03, Supabase −0.08) both discarded; the plan's stop rule (two consecutive failures) fired, so c3–c7 were not run and **the pre-tuning defaults are the winner**. Trials archived under `evals/results/docsqa/tuning/`, ledger `TUNING.md`, the page has the section. The discarded variants' vectors were dropped (`mda rebuild --embeddings --drop-other-models`) and the stores compacted, so the frozen artifacts are unchanged. Whether the five unrun search-time candidates get an explicitly labelled exploratory extension is an owner decision (a change to the pre-declared protocol; nothing from it could become the greedy winner).
- **Pooled-label diagnostic** (four projects, Fable judge, ≈ $27 list-price equivalent): about one in seven sampled unlabelled pages answers its question; with the pooled labels over the judged questions qmd full gains more than mda on GitHub Docs (0.422 → 0.556 vs 0.422 → 0.444) and Prisma (0.324 → 0.529 vs 0.294 → 0.500), ties Supabase (0.667), stays behind on Tailwind (0.76 vs 0.84). Section on the page; `<project>/pool/dev-column.json`.
- **Pooled labels**: `scripts/eval/pool.sh sample <project>` (100 unlabelled top-5 pairs, seeded, round-robin over the eight arms; Tailwind sample committed) and `pool.sh judge <project> dev fable` (`claude -p --model claude-fable-5-1`, JSON schema, frozen 0/1/2 rubric, ≈ $0.07 equivalent per pair; piloted on one pair). Astra's judgments go through the Codex CLI at M5 (`codex exec` needs `--skip-git-repo-check` or a trusted directory).

## 5d. Post-stop exploration (branch `feat/bench-post-stop`, 2026-09-24)

- **Decision** (owner: "decide with codex about c3 to c7"; Codex thread `01a0d31e-52bb-72f1-b3fb-4531737d6dd5`, record `docs/reviews/codex/2026-09-24-post-stop-c3-c7.md`): option C — run c3–c7 once each against the unchanged baseline, information only, never adopted this release; plan §3 carries a dated amendment. Rationale: the 0.01 screen is ≈ 0.2 SE of the baseline objective (bootstrap SE 0.0486; one Supabase question = 0.021 of the objective), a family-based stop reset after seeing the data would reset the fishing budget, and the 0.02 guardrail permits no net loss of one question anywhere (1/49 > 0.02).
- **Code**: `mda eval --compare <label> <baseline> <candidate>…` (`mda_core::eval::paired`: wins/losses/ties, unrounded deltas, within-project paired bootstrap with the objective resampled jointly, SplitMix64 seed 20260922, 5,000 draws; refuses differing question sets, duplicates, empty projects); `scripts/eval/tune.sh explore post-stop-<cN> key=value` (refuses a moved base or another name, unrounded screen and guardrail, decisions `screen-pass-not-adopted` / `screen-fail-not-adopted` / `invalid-not-adopted`, `compare.json` archived, `keep` refuses) and `tune.sh explore-table` (page table from the archives).
- **Results** (dev, information only; `TUNING.md` post-stop table): c3 `search_questions_weight=3` +0.0208 [0, +0.0625] 1/0 screen-pass; c4 `search_raw_weight=0.7` +0.0210 [−0.0198, +0.0727] 3/1 guardrail fail (Tailwind −0.04); c5 `search_rrf_k=30` +0.0310 [0, +0.0778] 3/0 screen-pass; c6 `fetch=60` and c7 `search_and_stopwords=true` leave every question's success@5 unchanged (ranks below the cutoff move on one GitHub Docs question each). **Nothing adopted**; the defaults are the M4 freeze. c3 and c5 are hypotheses for a future, separately declared evaluation.
- **Codex code review** of the paired module and `explore` (`docs/reviews/codex/2026-09-24-post-stop-compare.md`, thread `01a0d33e-9d1e-76f0-a52c-86036f887a80`): 4 findings, all fixed (nearest-rank percentile ⌈N·q⌉, `trial`/`keep` refuse `post-stop-*` names, a failed comparison keeps its observations and marks both decision records invalid, wording "unchanged success@5" not "unchanged retrieval"); local and archived baseline summaries are checked for agreement before an `explore`.
- Base is still empty (`tune.sh base` → fingerprint `e3b0c44298fc1c14`); the stores are unchanged (the trap restored every config).

## 5e. M4 — T1 published (branch `feat/bench-m4`, 2026-09-24/25)

- **Done**: `mda eval --interval` (per-row bootstrap intervals) and `--candidate-run` for `--compare`; `results_dir(table)` in lib, table-aware preflight (T1 rows under `evals/results/docsqa/T1/<project>/`, split test, `T1/FROZEN.md`), final-freeze Analysis lines, `pool.sh` `TABLE=T1` + Astra judge through `codex exec --output-schema --json` (works: the last `agent_message` item is the JSON), `scripts/eval/t1.sh run|latency|table|latency-table|target` (resumable, freeze check under a lock), `qmd_fingerprint` fail-closed with a busy timeout (two concurrent checks contended once), graphify driver drops sourceless nodes (empty path refused by the scorer). The timeout test's margin widened (6 s) after four failures under build load. **Final freeze written and committed before any row** (`T1/FROZEN.md`, re-written twice for driver fixes with identical inputs; the last source commit is in the file).
- **Runs**: two detached chains, logs in the session scratchpad `m4/chain-cpu.log` and `m4/chain-gpu.log` (`~/.cache/markdownattractor/bench/t1-runs/<project>/` holds the drive work; committed observations land under `T1/<project>/`). CPU arms (mda, BM25-over-files, graphify, graphify-haiku) take minutes; the qmd arms ≈ 4.5 h for the four projects. Then `t1.sh latency <p>` alone on the machine, the pooled column (both judges), the four preflights (`preflight.sh T1 <p>`), the tables, the page, runbook §7.1, the Codex M4 pass triage, PR, merge.
- **T1 done** (`docs/benchmarks.md` § T1; runbook §7.1 with the expected hashes): success@5 hybrid / qmd full: Tailwind 0.702 / 0.681 (5/4), Supabase 0.429 / 0.381 (2/1), Prisma 0.413 / 0.365 (11/8), GitHub Docs 0.483 / 0.460 (13/11); every paired interval includes 0. BM25-over-files 0.766 on Tailwind (6/3 against hybrid), 0.30 on the two large corpora. Latency: mda 0.2–0.7 s warm median, qmd 42–47 s (Supabase's 636 ms is qmd's `llm_cache` serving re-asked questions, disclosed). Pooled column: 100 pairs/project, Fable + Astra, agreement 0.89–0.96 exact; hybrid ahead on Tailwind/Prisma, tie on Supabase, behind qmd on GitHub Docs (0.559 vs 0.593).
- **Codex M4 pass** (`docs/reviews/codex/2026-09-24-bench-m4.md`): 10 findings, all fixed before any table (pooled sampler bug also affected the M3 diagnostic → correction note on the page). The freeze was re-written five times during the run for script fixes with identical inputs; every arm's manifest names its commit.
- **Preflights**: Tailwind, Prisma, GitHub Docs 15/15; Supabase fails one activation probe (graphify-haiku, question `supabase-13977`, two attempts, both committed) — published with the disclosure, no third attempt. Owner may overrule (rerun the probe or drop the row).
- **Lessons** (aha): never edit a script a detached run executes; batch script fixes before a freeze; the qmd LLM cache shortens re-asked warm queries; the `-0` in the table is a nearest-rank zero.

## 5f. M5 — the T2 harness (branch `feat/bench-m5`, 2026-09-25)

- **Built**: `mda eval --export-questions` (seeded, category-stratified sample with reference answers; only GitHub Docs has categories), `mda eval --analysis` (`mda_core::eval::analysis`, manifest-validated, per-metric savings, `--no-manifest` diagnostic), `scripts/eval/t2.sh run|status|grade|analysis` (manifest first with hashes, row files, `T2_JOBS/T2_TIMEOUT/T2_RETRIES`, exact per-turn tokens from `message_delta` with `--include-partial-messages`, clipped + signed source tokens, grounding on whole cited pages in batches with the declared citation rule, versioned grades, `--adjudicated`), `scripts/eval/panel.sh regrade|cards` (Fable via `claude -p`, Astra via `codex exec --output-schema --json --ephemeral`; declared trigger; adjudicated grades; card audit on "raw → iso (precision)" with coverage validation), `lib.sh arm_launch` (probe and runner share it), `scripts/eval/tests/failure-matrix.sh` (10 checks, `make check`). Runbook §5c has every command and policy.
- **Codex M5 pass** (`docs/reviews/codex/2026-09-25-bench-m5.md`): 11 findings, all fixed; the panel's jq had three quoting bugs found only by running it (aha: never publish a jq aggregation untested on stored judgments).
- **Pilot** (dev, 5 q × 4 projects × 1 run; scratch `m4/chain-pilot*.log`; run dirs `~/.cache/markdownattractor/bench/t2-pilot/<project>/`): 132 runs/h, 0 failures, $0.05/run; mda median 2.2K source tokens / 3 calls / 12 s vs grep 4.4K / 6 / 17 s, qmd 3.7K / 2 / 45 s, graphify 6.8K / 5 / 17 s. Grounding: 20–100% per arm/project after the citation-rule fix (before it, qmd was 0% from `<collection>/<path>` citations). Panel: Fable/Astra exact 0.38–0.5, within-one 0.63–1.0; Astra grades lowest; the trigger fired on 10 of 32. Cards (Prisma, 20): 2.4% unsupported per member, agreement 0.98.
- **Decisions carried in**: T1 stands (Supabase graphify-haiku probe disclosed); page-level aggregation is a declared hypothesis for a future dev-split round; rule 0.9 amendment accepted (cold servers per run).
- **Open before M6**: the grounding rubric (STATUS Next 2); the M6 budget (≈ 8 h runs + ≈ 9 h grading at one job; `T2_JOBS` untested above 1).

## 5g. M6 — T2 and T3 published (branch `feat/bench-m6`, 2026-09-25/26)

- **Decision** on "go M6": the grounding rubric narrowed to factual claims about the product, declared in plan §2.4 before the freeze (rationale: rule 0.8's intent is fabrication, not explanation).
- **Freeze** `T2/FROZEN.md` (samples as ids + hashes, models, harness and rubric hashes, parameters), committed before any run; re-written once (t3.sh added under it; inputs identical) — the aha about script commits during a frozen run bit again; the four preflights were rerun under the second freeze and pass.
- **Runs**: 1,002 answers, 0 failures, 6 retries after the 12-turn cap (declared), 7.4 h at one job (135 runs/h), $49.74. Grading ≈ 4.5 h; panel 4 × 30 × 2; cards 4 × 100 × 2 (≈ 2.5 h).
- **Result**: quality at parity (mda within 0.1 of grep everywhere; vs qmd +0.20 Tailwind, −0.14 Supabase, −0.52 Prisma, −0.08 GitHub; all intervals include 0); gate (c) fails everywhere → no savings claim; mda cheapest run on every project; mda fewest source tokens on 3 of 4 projects (grep 5% fewer on Tailwind). Panel: Astra 2–3 points below Sonnet/Fable, trigger on 18–28 of 30 — a calibration finding for any reuse. Cards: 0.4–3.8% unsupported, agreement ≥ 0.987. T3: `t3.sh table`; mda $4.29–4.66 per 1K sections; graphify Sonnet did not complete on the two large corpora ($73.54 and $82.77 spent).
- **Archived** under `evals/results/docsqa/T2/<project>/` (answers replaced by sha256; grades regenerate the analysis byte for byte), `T3/sizes.json`; run directories stay under `~/.cache/markdownattractor/bench/t2/`.

## 6. Next steps, in order

1. If the preflight reports are not all committed and green: read `~/.cache/markdownattractor/bench/preflight/development/<project>/<ts>/` and the log at the session scratchpad (`preflight-all.log`), fix, re-freeze (`scripts/eval/freeze.sh --protocol development --note …`, commit, `--check`), rerun. Then commit `evals/results/docsqa/preflight/`, open the PR, merge on eleven green jobs, `git checkout main && git pull`.
2. **M2** (plan §5): `scripts/eval/arms/qmd.sh` (per project: `qmd --index <p> collection add <checkout> --name <p> --mask '**/*.{md,mdx,markdown}'`, `qmd --index <p> embed`, coverage = `qmd ls` count vs corpus pages; driver: `qmd --index <p> query "<q>" --format json -n N --full-path`, N raised until ten distinct pages or exhaustion → `{question_id, paths[], truncated}`; the MCP request captured for the table; `--no-rerank` ablation; `qmd search` BM25 row; build times), `scripts/eval/arms/graphify.sh` (`/graphify <checkout>` from a `claude -p` session with the docs model recorded, `graph.json` archived and hashed under `evals/results/docsqa/arms/`, `query_graph` driver with the frozen node → file mapping), `scripts/eval/bm25-files.sh`, `scripts/eval/mcp-time.sh`; `evals/results/docsqa/arms/<arm>.json` (version, resolved models, effective configuration) which `freeze.sh` already folds into `FROZEN.md`'s Arms section and `preflight.sh` reads for the required probes; extend `probe.sh` with the qmd and graphify arms. Exit: preflight passes for every arm on every project.
3. **M3–M4** as in the plan; the tuning hypotheses from §4 above.
