# STATUS (updated 2026-09-25 by Claude)
**Start here after compaction: `docs/handoffs/2026-09-23-session4-final-handoff.md` (then the session-3 handoff for releases and recipes).**
Phase: benchmarks — **M1–M3 merged (PRs #22–#26); M4 on `feat/bench-m4` (PR open): T1 published on the test split under a final freeze — every arm, intervals, paired target, latency, two-judge pooled column, four preflights; tuning knobs in config, the greedy loop ran (stop rule after the two embedding-text candidates; pre-tuning defaults win), pooled-label diagnostic on the four dev splits**; Phases 0–4 shipped (v0.1.1)        Active plan: docs/plans/2026-09-benchmarks.md (rules) + docs/plans/2026-09-benchmark-execution.md v3.1 (milestones, competitor profiles, three Codex passes) · runbook evals/benchmark_it_with_claude.md
North star reminder: G6 100% grounded metadata · G1 < 1 s save→raw-searchable, p50 < 15 s save→card · G5 recall@5 ≥ 0.85

## Done (last 5)
- **Benchmark M4 — T1 published (2026-09-24/25, `docs/benchmarks.md` § T1, runbook §7.1)**: final freeze `T1/FROZEN.md` before any row; test split once for eight arms on four projects; success@5 hybrid vs qmd full 0.702/0.681, 0.429/0.381, 0.413/0.365, 0.483/0.460 (target met on every point estimate, every paired interval includes 0); BM25-over-files beats hybrid on Tailwind (0.766, 6 wins/3 losses) and trails by 0.11–0.18 on the large corpora; qmd 42–47 s per query vs mda 0.2–0.7 s; pooled column by Fable+Astra (agreement 0.89–0.96). Codex M4 pass: 10 findings fixed before any table. New: `mda eval --interval`, `--candidate-run`, `t1.sh`, table-aware preflight and freeze, Astra judge, BM25 fingerprints.
- **Post-stop exploration c3–c7 (2026-09-24, decided with Codex, information only)**: plan §3 amended; `tune.sh explore` (never adoptable) and `mda eval --compare` (paired wins/losses, within-project paired bootstrap, SplitMix64). Two screen-passes (RRF k 30 +0.031, 3/0; `questions_answered` weight 3 +0.021, 1/0), one guardrail fail, two no-ops (fetch 60, stop-words); every interval includes zero; **defaults unchanged**, M4 freezes them (`docs/benchmarks.md`, `TUNING.md`).
- **Benchmark M1 (execution plan §5)**: cards of the four corpora committed (`evals/results/docsqa/cards-0.1.1-<project>.json` + provenance, `model.sha`); `mda eval … --export-cards` / `--arm-output` (external-arm scorer, same page rule, no latency column); `scripts/eval/freeze.sh` (development `FROZEN.md`, `--check`), `preflight.sh` (frozen inputs, binary, model, store, exact regeneration, reconstruction from the committed cards, coverage per arm, three probes per arm with traces), `probe.sh`, `table.sh`. **Development rows at full coverage** (dev split): success@5 raw → cards → hybrid: github-docs 0.306 → 0.388 → 0.408, prisma 0.216 → 0.297 → 0.297, supabase 0.333 → 0.417 → 0.500, tailwind-css 0.600 → 0.840 → 0.800 (`docs/benchmarks.md`); adapter latency 0.5–1.6 s/question is the loudest number.
- **DocsQA adapter + ingestion gate**: `mda eval --dataset docsqa` (page-level success@5/MRR@5/nDCG@10, coverage and evidence-anchor report, seeded dev/test/holdout split, in `mda_core::eval`); all four projects at 100% label coverage, anchors 100/100/98/82% (GitHub Docs' gap is rendered Liquid includes); first axis-A row (raw lexical, dev split) is the floor: success@5 0.31/0.22/0.33/0.60. Carding rate through the Claude Code login: ≈ 1.1 sections/s, ≈ $175 equivalent for all four corpora (`docs/benchmarks.md`).
- **B0b lean MCP payload**: `mda_search` returns five hits without ranking diagnostics; golden-corpus median source tokens 1,304.5 → 762.5 at 11/12 parity, but more turns; exploratory (`docs/benchmarks.md`). `ab.sh`/`grade.sh` run through `claude -p`, fail loud, check a run manifest (Codex review 6/6 triaged).

## Next (in order)
1. **M4 close**: merge the T1 PR on eleven green jobs; then `git checkout main && git pull`.
2. **M5+**: T2 harness (resumable runner, transcript tokens, grounding, `--analysis`, `panel.sh` with the Astra route, `--export-questions`), pilot, owner decision on the rule-0.9 amendment; the graphify-haiku chunk audit and the T3 one-edit measurement are owner calls.

## Blockers / open questions
- Repo is **public** since 2026-09-22 (history rewritten to scrub workspace ids; old SHAs in docs are stale). Actions budget is $0 by owner choice; public-repo minutes are free. Earlier today GitHub Actions stopped starting jobs: "The job was not started because recent account payments have failed or your spending limit needs to be increased" (Billing & plans → Actions spending limit; the release dry run's macOS/Windows/arm64 minutes are billed at multipliers). PR #10 (`docs/phase4-wrap`: two release-workflow fixes + handoff) has no CI until that is fixed; merge it after a green job list, then re-run the release dry run.
- API key: the owner decided on 2026-09-22 that it does not need rotating; it lives in `~/.config/markdownattractor/env`.
- Hybrid query latency (≈ 50 ms in-process, 257 ms as a cold process) misses the plan's 30 ms budget; lexical meets it. Recorded, not hidden.
- §13 open decisions left: commit `cards/` or not. (Embeddings and MCP-as-subcommand decided in ADR-0004; daemon per-root in ADR-0003.)

## Last Codex review: 2026-09-24 (docs/reviews/codex/2026-09-24-bench-m4.md: the M4 pass on scorer, drivers and freeze — 10 findings, all fixed before any T1 table: pooled sampler, preflight's final-protocol check, latency files, eligibility on original labels, BM25 fingerprints and the missing Tailwind record, attempts and manifests, driver rows archived, named panel with agreement, Astra completion/schema checks, one excerpt routine). Before it: 2026-09-24-post-stop-compare.md: paired module and `explore` — 4 findings, all fixed: nearest-rank percentile, `post-stop-*` namespace closed to `trial`/`keep`, failed-comparison bookkeeping, wording. Same day: 2026-09-24-post-stop-c3-c7.md: the c3–c7 decision — option C, information only, no adoption). Before it: 2026-09-23 (docs/reviews/codex/2026-09-23-bench-m3-tuning.md: tuning knobs and loop — 8 findings, all fixed: combined embedding variant, finite bounds, restoration trap, persisted base, archived trials). Before it: 2026-09-23-bench-m2.md: competitor arms, drivers, probes, freeze — 9 findings, 8 fixed + 1 in part; artifact hashes in the freeze, completion verdict from the process, the graphify-haiku reading corrected). Earlier today: 2026-09-23-build-cost-claim.md (build cost is a design property, not a savings claim until T3 at a matched model), 2026-09-23-bench-m1.md. — M1 export, arm scorer, freeze, preflight, probes: 10 findings (5 High), 9 fixed + 1 in part; the preflight is now a real gate (interrupted runs fail, model links hashed, successful tool results required, regeneration from archived page lists). Execution-plan pre-mortem: 2026-09-23-execution-plan.md.
