# Changelog 2026

## 2026-08-14

- Checkout: Apple Pay is available in the EU storefronts.
- API: `GET /orders` gained `?since=` and `?until=` filters.
- Fixed: order confirmations were delayed when the email provider throttled us.

## 2026-05-02

- Search: the product search index rebuilds every hour instead of nightly.
- Workers moved to their own repository (ADR 0009).

## 2026-01-20

- Deploys: `deployctl release` runs the smoke suite against the idle colour before the flip.
